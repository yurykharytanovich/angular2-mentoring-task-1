import { MyOrderByPipe } from './my-order-by.pipe';

describe('MyOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new MyOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
