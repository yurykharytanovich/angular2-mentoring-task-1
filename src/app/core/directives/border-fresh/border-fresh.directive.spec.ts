import { BorderFreshDirective } from './border-fresh.directive';

describe('BorderFreshDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderFreshDirective();
    expect(directive).toBeTruthy();
  });
});
